FROM python:3.11-slim
WORKDIR /app

# Copy requirements
COPY packages/skills/requirements.txt .

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy source code
COPY packages/skills ./

EXPOSE 8001
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8001"]
