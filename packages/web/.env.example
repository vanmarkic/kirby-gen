# API Configuration
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000

# Authentication (optional for local development)
# Leave empty if AUTH_ENABLED=false on API server
VITE_AUTH_TOKEN=
